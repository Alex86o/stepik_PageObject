# stepik_PageObject
Educational Project about base skill of Seleniun using Python. Using test runner pytest.
Writing scripts for searching elements on page(buttons, text, forms), go to page, filling form, work with several  windows, work with expectations, makrs, parametrizes, OOP(write classes, using principals heritage, methods), use fuxtures.
Base skills of git.
